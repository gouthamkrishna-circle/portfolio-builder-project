# Dependencies
# This folder contains all the installed packages and can be very large.
# It should always be ignored.
/node_modules

# Uploads
# This folder contains user-uploaded files. It's best to ignore this
# so that local uploads don't get mixed with production uploads.
/uploads

# Logs
# These files are generated by npm or yarn for debugging.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment Variables
# This file would contain secrets like API keys and database passwords.
.env

# Operating System files
.DS_Store
Thumbs.db